export interface tiposIdentificaciones {
  codigo: string;
  descripcion: string;
}
export interface ubicaciones {
  codigo: string;
  descripcion: string;
}

export interface discapacidades {
  codigo: string;
  descripcion: string;
}

export interface etnias {
  codigo: string;
  descripcion: string;
}
export interface sexos {
  codigo: string;
  descripcion: string;
}

export interface generos {
  codigo: string;
  descripcion: string;
}

export interface parentescos {
  codigo: string;
  descripcion: string;
}
